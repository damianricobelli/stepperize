/**
 * Type-safe, unstyled primitive components for Stepperize.
 *
 * Use the primitives returned by `defineStepper([...]).Stepper` when you want
 * accessible structure, data attributes, keyboard navigation, and full control
 * over the rendered elements.
 */

export type { StepperPrimitives } from "./create-stepper-primitives";

export { createStepperPrimitives } from "./create-stepper-primitives";
export type {
	ActionsProps,
	ContentProps,
	ControlledStep,
	DescriptionProps,
	IndicatorProps,
	ItemProps,
	ItemsProps,
	ListProps,
	NextProps,
	PrevProps,
	PrimitiveProps,
	RenderProp,
	RootProps,
	SeparatorProps,
	StepStatus,
	TitleProps,
	TriggerProps,
} from "./types";
