---
title: Define
description: Create a fully-typed stepper with defineStepper
---

`defineStepper` creates a stepper from your step definitions. You pass one object per step (as separate arguments). It returns typed `Stepper` (primitives), `Scoped`, `useStepper`, and `steps`.

## Basic usage

```tsx twoslash
import { defineStepper } from "@stepperize/react";

const { Stepper, Scoped, useStepper, steps } = defineStepper(
  { id: "shipping", title: "Shipping", description: "Enter your address" },
  { id: "payment", title: "Payment", description: "Payment details" },
  { id: "confirmation", title: "Confirmation", description: "Review order" }
);
```

Each step must have a unique `id`. You can add any custom properties (`title`, `description`, `icon`, etc.) for your UI.

## Return value

| Property | Type | Description |
| -------- | ---- | ----------- |
| `steps` | `Steps` | The array of step objects you passed (same order as arguments) |
| `useStepper` | `(props?: { initialStep?, initialMetadata? }) => Stepper<Steps>` | Hook for state, navigation, lookup, flow, metadata, lifecycle ([Hook](./hook)) |
| `Scoped` | `(props: ScopedProps<Steps>) => ReactElement` | Provider for stepper context; props: `initialStep?`, `initialMetadata?`, `children` ([Scoped](./scoped)) |
| `Stepper` | `StepperPrimitives<Steps>` | Type-safe UI primitives: Root, List, Item, Trigger, Title, Description, Indicator, Separator, Content, Actions, Prev, Next ([Primitives](./primitives)) |

## Next steps

- [Primitives](./primitives) – Build the stepper UI
- [useStepper](./hook) – State, navigation, lookup, flow, metadata
- [Scoped](./scoped) – Share stepper state
- [Lookup](./lookup) – Step lookup (via `stepper.lookup` or `generateStepperUtils` from core)
