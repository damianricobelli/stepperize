---
title: Define
description: Create a fully-typed stepper with defineStepper
---

`defineStepper` creates a stepper from your step definitions. It takes only the steps array and returns typed components, hook, and utilities.

## Basic usage

```tsx twoslash
import { defineStepper } from "@stepperize/react";

const { Stepper, Scoped, useStepper, steps, utils } = defineStepper(
  { id: "shipping", title: "Shipping", description: "Enter your address" },
  { id: "payment", title: "Payment", description: "Payment details" },
  { id: "confirmation", title: "Confirmation", description: "Review order" }
);
```

Each step must have a unique `id`. You can add any custom properties (`title`, `description`, `icon`, etc.) for your UI.

## Return value

| Property | Type | Description |
| -------- | ---- | ----------- |
| `steps` | `Steps` | The array of step objects you passed |
| `utils` | `Utils<Steps>` | Pure helpers for steps ([Utils](./utils)) |
| `useStepper` | `(props?) => Stepper & TransitionMethods & TransitionState` | Hook for state and navigation ([Hook](./hook)) |
| `Scoped` | `Component` | Provider for stepper context ([Scoped](./scoped)) |
| `Stepper` | `StepperPrimitives<Steps>` | Type-safe UI primitives ([Primitives](./primitives)) |

## Next steps

- [Primitives](./primitives) – Build the stepper UI
- [useStepper](./hook) – State and navigation
- [Scoped](./scoped) – Share stepper state
- [Utils](./utils) – Pure step helpers
